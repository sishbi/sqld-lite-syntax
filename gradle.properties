group=sishbi
# Always the version of the next release, plain MAJOR.MINOR.PATCH. Never a -SNAPSHOT suffix: the
# release draft is tagged with this string, and a suffix would publish to a Marketplace channel of
# that name instead of the default one. The release workflow bumps the patch after each release;
# raise the minor or the major by hand when a change earns it.
version=0.1.3
# Opt-out flag for bundling Kotlin standard library -> https://jb.gg/intellij-platform-kotlin-stdlib
kotlin.stdlib.default.dependency=false
# Enable Gradle Configuration Cache -> https://docs.gradle.org/current/userguide/configuration_cache.html
org.gradle.configuration-cache=true
# Enable Gradle Build Cache -> https://docs.gradle.org/current/userguide/build_cache.html
org.gradle.caching=true
