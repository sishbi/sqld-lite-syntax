CREATE TABLE book_loans (
  loan_id TEXT NOT NULL PRIMARY KEY,
  member_id TEXT NOT NULL,
  status TEXT NOT NULL,
  created TEXT NOT NULL
);

CREATE INDEX book_loans_member_id
ON book_loans(member_id);
